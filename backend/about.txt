ruff check --fix

ruff format //выравн отступы и оброезает длинные строки, правильно расставляет пробелы сортирует и группирует импорты убирает лишние пустые строки

------------------
использовать 4 пробела вместо таба
макс длина строки 88
пробелы вокург операторов
импорты(между группами -одна пустая строка)

!2 пустые строки между функциями и классами верхнего уровня
1 пустая строка между методами внутри класса

докстринг в начале функций (почему а не что, полные предложения с заглавной буквы)

переменные artwork_catalog
классы UserPage
const в апперкейсе